This example does not use a web.xml file.

Instead, it uses an ApplicationConfig class.

Here to compile you must do:
./compile_hello
./compile_ApplicationConfig

Then make the war file:
./create_the_war

THen deploy on Glassfish
